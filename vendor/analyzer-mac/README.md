# macOS analyzer runtime

This directory is populated by:

```bash
npm run package:analyzer-mac
```

Run that command on macOS before `npm run build:mac`.
The generated executable is intentionally not committed.
