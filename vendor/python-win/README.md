# Windows Python runtime

This directory is populated by:

```bash
npm run package:python-win
```

Run that command on Windows x64 or in Windows CI before `npm run build:win`.
The generated runtime is intentionally not committed.
